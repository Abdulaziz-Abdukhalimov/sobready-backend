package com.sobready.backend.enums;

/**
 * Matches your React enum:
 * enum MemberStatus { ACTIVE = "ACTIVE", BLOCK = "BLOCK", DELETE = "DELETE" }
 */
public enum MemberStatus {
    ACTIVE,
    BLOCK,
    DELETE
}
